# NearSplitter Frontend Environment Variables
# Copy this file to .env.local and fill in your values

# =============================================================================
# REQUIRED VARIABLES
# =============================================================================

# The NEAR smart contract account ID for NearSplitter on the selected network
# Example: nearsplitter.testnet
NEXT_PUBLIC_CONTRACT_ID=nearsplitter.testnet

# =============================================================================
# OPTIONAL VARIABLES
# =============================================================================

# Network selection: "testnet" or "mainnet"
# Default: "testnet"
# NEXT_PUBLIC_NEAR_NETWORK=testnet

# RPC endpoint URL for NEAR blockchain queries
# Default: Uses Pagoda RPC (fast, rate-limited to 2 req/s)
# For development with higher throughput, run a local node or use a dedicated service
# See: https://docs.near.org/api/rpc/introduction
# NEXT_PUBLIC_NEAR_RPC_URL=https://rpc.testnet.near.org

# =============================================================================
# SETUP GUIDE
# =============================================================================

# 1. Contract Setup:
#    - For testnet: Deploy with: cd contracts && make testnet-deploy
#    - For mainnet: Perform security audit before deploying
#    - Update NEXT_PUBLIC_CONTRACT_ID to your deployed contract

# 2. Storage Registration:
#    - Check if contract has storage: nears storage_deposit at CONTRACT_ID
#    - If needed, call storage_deposit() on the contract

# 3. Wallet Connection:
#    - Get a testnet wallet: https://wallet.testnet.near.org
#    - Get a mainnet wallet: https://wallet.near.org

# 4. Development Mode:
#    - Run: pnpm dev
#    - Visit: http://localhost:3100
#    - Frontend will show balance and circle info once connected to wallet

# 5. Production Deployment:
#    - See DEPLOYMENT.md for secure production setup
#    - Enable Content Security Policy (CSP) headers
#    - Use mainnet contract ID and verified RPC endpoints
#    - Never commit .env.local to git (use .env.local.example for documentation)
